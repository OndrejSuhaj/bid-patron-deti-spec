# FLW0030 — Transaction PAID → campaign recommendation (dormant)
> AR:FlowMiner dossier · 2026-07-01 · source FlowID FL028 · see [../flow-index.md](../flow-index.md)

## A. Header
- FlowID: FL028 (dossier FLW0030)
- Flow Name: Transaction PAID → campaign recommendation (dormant)
- Primary SRV: SRV0006 (CampaignRecommendation-Processor — marked **Transitional / dead cron** in [../../spec-draft/SRV-target-list.md](../../spec-draft/SRV-target-list.md):56)
- Trigger Evidence: `transaction/src/Entity/TransactionEntity.php:696` calls `dispatchStatusChangeEvent()` from `postSave()` (`:633`); the method body (`:862-865`) that would build+dispatch `TransactionUpdateEvent::UPDATE_EVENT` is **fully commented out**. Intended subscriber: `campaign_recommendation/src/EventSubscriber/CampaignRecommendationSubscriber.php:25` `onTransaction()`.
- Confidence Level: Confirmed (dormancy is confirmed on multiple independent grounds; behavior of the code *if reactivated* is Partial/Hypothesis where noted)

## B. Behavior Digest

### Dormancy verdict (Confirmed — flow is currently inert on 5 independent grounds)
The START hint is accurate. This flow **cannot execute today**. Every ground below is sufficient on its own:
1. **Event dispatch commented out.** `TransactionEntity::dispatchStatusChangeEvent()` (`transaction/src/Entity/TransactionEntity.php:862-865`) has both the `new TransactionUpdateEvent($this)` and `$dispatcher->dispatch(...)` lines commented. `postSave()` calls the method (`:696`) but it is a no-op. Grep confirms these two lines are the **only** references to `TransactionUpdateEvent` outside the event class and the subscriber (no other dispatcher).
2. **Module not installed.** `campaign_recommendation` is **absent** from `config/core.extension.yml` (grep count = 0; `transaction`, `account`, `campaign` are all present with weight `0`). If Drupal does not have the module enabled, the subscriber is never registered and its queue workers/commands are unavailable.
3. **Classifier service commented out.** `campaign_recommendation/campaign_recommendation.services.yml` is entirely commented (`:1-25`): the `campaign_recommendation.classifier` service (`Phpml\Classification\SVC`) and all its parameters are disabled. `PatronUser::doTrainModel()` (`account/src/PatronUser.php:639`) fetches `\Drupal::service('campaign_recommendation.classifier')` → would throw (service not defined).
4. **php-ml library absent.** No `php-ml`/`Phpml` in root `composer.json`/`composer.lock`, no `vendor/php-ml` or `*Phpml*` directory anywhere in source (Patronus is scrubbed of vendor, but the dependency is also not declared). `use Phpml\Classification\Classifier` in `PatronUser.php:11` would be unresolvable.
5. **Feature-set query returns nothing.** `CampaignEntity::buildSetPredict()` (`campaign/src/Entity/CampaignEntity.php:1479-1506`) has its entire body commented out and returns `NULL` — so `CampaignScoringQueue` would call `CampaignEntity::buildSetPredict()` and pass `NULL` to `buildScores()`. Additionally the storage field `campaign_recommendation` (the `campaign_entity_reference` custom field type that stores the results) is **commented out** in `account/account.module:205-213`, so there is no DB column to persist recommendations into.

### Trigger (dormant contract — what WOULD fire)
- Trigger: On `TransactionEntity::postSave()`, after every transaction save, `dispatchStatusChangeEvent()` would dispatch `TransactionUpdateEvent::UPDATE_EVENT` (`transaction.update.event`, constant at `transaction/src/Event/TransactionUpdateEvent.php:13`) carrying the transaction entity.
- Preconditions (WOULD-fire): subscriber runs on every dispatch; it self-gates on `transaction.ext_status == 'PAID'` and non-empty owner (`getOwnerId()`) — `CampaignRecommendationSubscriber.php:27-29`.

### Main Steps (dormant contract — ordered, each cited)
1. `TransactionEntity::postSave()` → `dispatchStatusChangeEvent()` (`transaction/.../TransactionEntity.php:696`) — **currently no-op**.
2. `CampaignRecommendationSubscriber::onTransaction()` reads `ext_status`; if `PAID` and owner present, enqueues one item `['uid' => $uid]` into queue `campaign_training_queue` (`.../CampaignRecommendationSubscriber.php:30-32`). Note: `createQueue()` call is commented (`:31`) — relies on queue existing (created by `campaign_recommendation_update_8001`, `campaign_recommendation.install:9-11`).
3. Queue worker `campaign_training_queue` (`CampaignTrainingQueue::processItem`, `.../QueueWorker/CampaignTrainingQueue.php:22`) loads the `user`, calls `$account->trainModel()`, `$account->save()`, logs duration, then enqueues `['uid' => $uid]` into `campaign_scoring_queue` (`:31-40`). Emits a `messenger` "process item" message (`:24`).
4. `PatronUser::trainModel()` (`account/src/PatronUser.php:574-593`): counts PAID transactions (`getNumberOfTransactions`, `:648`), builds positive samples (`buildPositiveSamples`, `:694` — raw SQL over `contact,aprofile,application,campaign,transaction`) and negative samples (`buildNegativeSamples(n = txCount * SAMPLES_MULTIPLIER=5)`, `:742`), trains via `doTrainModel()` → `campaign_recommendation.classifier` (php-ml SVC), stores serialized model in the `model` base field (`setModel` → `serialize()`, `:566`).
5. Queue worker `campaign_scoring_queue` (`CampaignScoringQueue::processItem`, `.../QueueWorker/CampaignScoringQueue.php:22`): loads user, `$campaigns = CampaignEntity::buildSetPredict()`, `$account->buildScores($campaigns)`, `$account->save()`, logs duration.
6. `PatronUser::buildScores()` (`account/src/PatronUser.php:783-816`): `getModel()` (unserialize the `model` field, `:547`), `model->predictProbability(campaigns)`, sort desc, rewrite `campaign_recommendation` multi-value field with `{target_id, weight=prediction['a']}` per campaign.
- Postconditions (WOULD-be): `user.model` holds serialized trained SVC; `user.campaign_recommendation` holds ranked campaign→weight rows. **Neither persists today** (field commented out; module/service absent).
- Side Effects (WOULD-be): 2 queue items per PAID save (train → score); `user` entity saved twice (once per worker); `campaign_recommendation` logger info entries; `messenger` messages. No emails, no money movement, no external HTTP.
- Integration Calls: **None external.** php-ml is an *in-process* PHP ML library (SVM), not a network integration; it is currently absent. No third-party API.
- Failure Modes (if reactivated): (a) `\Drupal::service('campaign_recommendation.classifier')` undefined → fatal in `doTrainModel` (`:639`); (b) `class Phpml\...\Classifier` not found → fatal at load of `PatronUser`/interfaces; (c) `buildSetPredict()` returns `NULL` → `array_values(NULL)`/iteration error in `buildScores`/scoring command; (d) `messenger()->addMessage()` inside a queue worker (`CampaignTrainingQueue:24`) is a CLI/queue-context smell; (e) unbounded scoring — `buildScores` iterates all active campaigns per user; (f) `unserialize()` of stored model (`getModel:552`) — object-injection / stale-class risk (Security) if a serialized blob ever existed.

## C. Data Footprint
- Entities Written (WOULD-be, none today): `user` (fields `model`, `campaign_recommendation`) — via `PatronUser::save()` in both queue workers and the two console commands. The `campaign_recommendation` field is commented out (`account/account.module:205`), so it has no storage; `model` field DOES exist (`account/account.module:185-190`, `string_long`).
- Entities Read: `transaction` (trigger + PAID counts + positive/negative sample SQL, `PatronUser.php:648-660`, `:694-777`), `campaign` / `contact` / `aprofile` / `application` (raw SQL joins in `buildPositiveSamples`/`buildNegativeSamples` and the commented `buildSetPredict`), `user` (loaded by uid in workers/commands).
- Constraints involved: sample cleaning in `doTrainModel` drops rows with empty/negative columns and a `time>1.1` heuristic (`PatronUser.php:616-635`); positive/negative label = `'a'`/`'b'`; `getNumberOfCampaigns` excludes `test=1` on production only (`:665-678`).
- Multi-tenant scope assumptions: **No CZ/RO/MD tenant scoping anywhere in this flow.** SQL and entity queries key only on `user_id`/`ext_status='PAID'`; campaign feature-set (commented) is global (`campaign_status='active'`, `MAX(gift_price)` across all campaigns). If reactivated, recommendations would cross tenant boundaries. (Boundary gap — Multi-tenant.)

## D. Evidence Block
- Controller paths: none (no route/controller; trigger is an entity lifecycle hook + Symfony event subscriber).
- Service methods: `PatronUser::trainModel/doTrainModel/getNumberOfTransactions/getNumberOfCampaigns/buildPositiveSamples/buildNegativeSamples/getModel/setModel/buildScores` (`account/src/PatronUser.php:547-816`); interface `account/src/PatronUserInterface.php`, `account/src/Entity/AccountEntity{,Interface}.php` (parallel copies, `trainModel`/`buildScores` at `AccountEntity.php:310,495`); `CampaignEntity::buildSetPredict` (`campaign/src/Entity/CampaignEntity.php:1479`, body commented).
- Repository usage: `entityQuery('transaction')` count (`PatronUser.php:652`); `Database::getConnection()->query()/queryRange()` raw SQL (`:695`, `:743`); `entityTypeManager()->getStorage('user')->load()` (`CampaignTrainingQueue.php:27`, `CampaignScoringQueue.php:25`); `\Drupal::database()->query()` in `getNumberOfCampaigns` (`:667`).
- Event listeners: `CampaignRecommendationSubscriber` subscribes `TransactionUpdateEvent::UPDATE_EVENT => onTransaction` (`.../EventSubscriber/CampaignRecommendationSubscriber.php:16`). Registered via `campaign_recommendation.services.yml` — **note the services.yml is entirely commented (`:1-25`)**, so even the subscriber service is undeclared. Event class `transaction/src/Event/TransactionUpdateEvent.php`.
- Async messages: Drupal core Queue API — queues `campaign_training_queue` (`QueueWorker/CampaignTrainingQueue.php`, `@QueueWorker id`) and `campaign_scoring_queue` (`QueueWorker/CampaignScoringQueue.php`). Queues created in `campaign_recommendation_update_8001` (`campaign_recommendation.install:6-11`). Chain: subscriber → training_queue → scoring_queue. No message broker; Drupal DB-backed queue (default).
- Config evidence: `campaign_recommendation.services.yml` (classifier SVC + kernel/cost/degree/gamma params) **all commented** (`:1-25`); `campaign_recommendation.info.yml` deps `[entity_reference]`; module **not in** `config/core.extension.yml`; `hook_cron` in `campaign_recommendation.module:7-26` **commented out** (the periodic re-scoring driver is dead too). Alternate manual entrypoints exist but depend on the (disabled) module: drush `campaign_recommendation:trainAll` (`Commands/CampaignRecommendationCommands.php:43`, enqueues all users — its `createQueue()` also commented), Drupal Console `train:all` (`Command/TrainCommand.php`) and `campaign:scoring` (`Command/CampaignScoringCommand.php`, gated on `scoring_queue=1`). Field type `campaign_entity_reference` (`Plugin/Field/FieldType/CampaignRecommendationItem.php`) adds a float `weight` column but its base-field usage on `user` is commented (`account/account.module:205-213`). db-models cross-ref: [../db-models.md](../db-models.md):26-27,30-31 (fields `campaign_recommendation` "conditional — only if module enabled", `model` serialized classifier).
