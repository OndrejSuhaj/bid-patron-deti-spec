# EN Lifecycle Evidence (Patronus) — evidence-only

> Generated by **AR:FlowMiner** · 2026-07-01 · batch 1. **Evidence only — NOT an EN document** (no invariants authored; for ENExtractor to reuse).
> Observed state changes per central entity, with code evidence + confidence. Source: [../evidence/flow/](../evidence/flow/) FLW0001–FLW0010.
> Confidence: Confirmed = direct code evidence · Partial = some steps not fully traced · Hypothesis = inferred.

## application  (base_table `application`; content_moderation `application_workflow`, ~66 states)
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (create) → `new` (+ initial `application_states` audit row) | `UserCreateForm::createApplication`; `ApplicationEntity::postSave` L222-224 | Confirmed | FLW0010, FLW0002 |
| any → target state (`state`+`moderation_state` in lock-step) | `ApplicationEntity::setState()` L292-310 | Confirmed | FLW0002 |
| → `to_check` ⇒ low-risk score recomputed (`scoring_low_risk*` via raw UPDATE) | scoring `ApplicationStatusUpdateSubscriber` L67-118 | Confirmed | FLW0001, FLW0002 |
| → `waiting_signature*` ⇒ acceptance-protocol contract + signing session; `acceptance_protocol` set | notification `ApplicationStatusUpdateSubscriber` L64-220 | Confirmed | FLW0001, FLW0002, FLW0008 |
| → `waiting_for_feetback*` ⇒ feedback + signing session | notification subscriber | Confirmed | FLW0001 |
| → `returned_new_patron` ⇒ `removePatron()` scrubs patron data | `ApplicationEntity::preSave` L181-183,1283 | Confirmed | FLW0002 |
| `contract` → `waiting_signature` → `contract_signed` | `ApplicationContractController` L398/448/493; `ContractFundraiserSignForm` L53,237 | Confirmed | FLW0008 |
| → `complete` when owning campaign completes | `CampaignEntity::preSave setApplicationComplete` | Partial | FLW0003 |
> Note: no transition-legality guard on `ChangeModStateForm`/`WorkflowStatusForm` (any state reachable). `Conflict`: `moderation_state` has no `baseFieldDefinitions()` entry (content_moderation-supplied) — see db-models.md.

## transaction  (base_table `transaction`; `ext_status` enum PENDING/PAID/CANCELLED/AUTHORIZED/REFUNDED)
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (create) → `PENDING` (+ `ext_trans_id` from gateway) | `v32/TransactionResource.php` L183,190,254,310 | Confirmed | FLW0006 |
| PENDING/AUTHORIZED → `PAID`\|`CANCELLED`\|`REFUNDED` (ComGate) | `comgate/.../TransactionStatusUpdate::update_status` | Confirmed | FLW0003 |
| → `PAID`/`PENDING`/`CANCELLED`/`REFUNDED` (Netopia IPN action map) | `NetopiaConfirmController` L70-141 | Confirmed | FLW0004 |
| PENDING → `PAID`\|`CANCELLED`\|`PENDING` (MAIB re-poll command=c) | `MaibController` L60-72 | Confirmed | FLW0005 |
| (recurring) new child → `PENDING` → `PAID` (optimistic) \| `CANCELLED` | `ComgateCron` L61/99/102; `NetopiaCron` L64/79/102 | Confirmed | FLW0007 |
| `is_email_sent` false → true on first PAID | `TransactionEntity` L612-624,760 | Confirmed | FLW0004 |
| overpayment split ⇒ new child transaction to transparent_account | `TransactionEntity::postSave` L663-689 | Confirmed | FLW0003, FLW0004, FLW0005, FLW0007 |
> Money side-effects (campaign recompute, email, Slack, recurring/voucher promote, ES enqueue) all fire from `TransactionEntity::preSave/postSave` on any save.

## transaction_recurring
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (create) → `status=0` (inactive) | `v32/TransactionResource.php` L206 | Confirmed | FLW0006 |
| 0 → 1 (activated) when linked txn PAID | `TransactionEntity::updateRecurringStatus` L697-711 | Confirmed | FLW0003, FLW0004, FLW0005 |
| token_id/token_expiration set from IPN (RO) | `NetopiaConfirmController` L157-163 | Confirmed | FLW0004 |
| due → charged (`last_recurring_payment=now`) on success | `ComgateCron` L93-94; `NetopiaCron` L83-84 | Confirmed | FLW0007 |

## campaign  (base_table `campaign`)
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| active → complete when `raised >= gift_price` | `CampaignEntity::preSave isCampaignReadyToComplete→setComplete` (via `$campaign->save()` in `TransactionEntity::postSave`) | Confirmed | FLW0003 |
| `campaign_status`/`gift_category` synced from application state | `ApplicationEntity::updateCampaignStatus/Category` L1176-1228 | Confirmed | FLW0002 |
| set-active (publish) | `campaign PublishController::setActive` (see FL044, not deep-mined) | Partial | — |

## contract  (base_table `contract`; revisionable)
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| created (draft) → manager-signed (html stamped) | `ContractEntity::signManager` L467 | Confirmed | FLW0008 |
| manager-signed → fundraiser-signed (digital_signature JSON + SHA-512 hash + QR confirmation PDF) | `ContractEntity::signFundraiser` + `createFundraiserSignatureConfirmation` L490,526 | Confirmed | FLW0008 |

## user / contact  (Identity & Party)
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (anonymous) → registered, role `fundraiser`\|`patron`\|`supporter` | `AccountService::register` L41-64; `updateUserRole` (payments) | Confirmed | FLW0010, FLW0003, FLW0006 |
| blocked → activated (side effect of `getUserMagicLink`) | `AccountService::getUserMagicLink` L274-279 | Confirmed | FLW0010 |
| contact (create) → person, `field_name=role` | `AccountService::register/ensureUser` `ContactEntity::create` L67-81,329 | Confirmed | FLW0010 |
| `contact.blacklist_type` written by scoring (`scoring_ok` ⇒ `wl_z`) | scoring flow (cross-context) | Partial | FLW0001 |

## application_session
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (create) → status=TRUE, interface per role | `ApplicationEntity::postCreate createPatron/FundraiserSession` L96-148 | Confirmed | FLW0010 |
| → deactivated (all sessions) when new state ∈ `invalidate_sessions` config | application_reaction `cancelSessions`→`ApplicationService::deactivateSessions` | Confirmed | FLW0001 |

## donation_confirmation / email
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| (none) → created/published (`status` default TRUE) | `DonationConfirmationEntity::create()->save()` form L330-344 / v32 L139-156 | Confirmed | FLW0009 |
| email (none) → archived per send | `APIMailingService::sendEmail EmailEntity::create()->save()` L217-230 | Confirmed | FLW0009, FLW0010 |

## voucher
| Transition | Evidence | Conf | Flow |
|---|---|---|---|
| status 0 (unapplied) → 1 (applied) + applied timestamp on PAID | `TransactionEntity::updateVoucherStatus` | Confirmed | FLW0003 |

> Gaps for ENExtractor: exact application `complete`/`completed_partly` source labels (Partial, FLW0003); `contact.blacklist_type` transition table (Partial); campaign publish/uncompleted transitions (not deep-mined — batch 2 FL044/FL045).
