# SRV Restructuring Actions

> Generated by **AR:SRVRestructurer** · 2026-07-01 · derived ONLY from existing SRV drafts (no code re-read).
> Companion: [SRV-architecture-map.md](SRV-architecture-map.md) · [SRV-target-list.md](SRV-target-list.md).
> Rule applied: an **Integration Adapter owns exactly one external boundary**; **Domain** holds no vendor calls; **Processors** hold no domain logic; **Orchestrators** hold no domain invariants.

---

## 1. SPLIT candidates

| # | Source SRV | Split into | Justification (from drafts) |
|---|---|---|---|
| S1 | **SRV0008** Payment Gateway Adapters | `ComGate-Adapter`, `Netopia-Adapter`, `MAIB-Adapter` (behind one `PaymentGateway` port) | 3 vendors in one SRV; each a distinct protocol/lock-in (arch-map §4). Adapter = 1 boundary. |
| S2 | **SRV0009** Bank Reconciliation | `BankReconciliation` (Domain: matching/VS), `BankMail-IMAP-Adapter`, `Moneta-AISP-Adapter`, `ComGate-TransferSync-Adapter` + `Reconciliation-Processor` (cron) | Mixes 3 inbound boundaries + reconciliation domain logic + cron. Adapter-masquerading-as-Domain. |
| S3 | **SRV0004** Identity & Registry Verification | `MVCR-DocValidity-Adapter`, `ARES-Registry-Adapter` | 2 gov boundaries in one adapter. |
| S4 | **SRV0014** Marketing, Analytics & Lead | `Mautic-CRM-Adapter`, `FacebookCAPI-Adapter`, `Analytics-Adapter` (GTM/Pixel); lead-intake → route to SRV0001 | 3 boundaries + cross-context lead creation (C8→C1). |
| S5 | **SRV0013** Transactional Messaging | `Messaging-Orchestrator` (L2), `Email-Adapter`, `WhoisXML-DomainCheck-Adapter` | Orchestration + delivery adapters + template resolution conflated (APIMailingService, USE_QUEUE=FALSE). |
| S6 | **SRV0017** Platform Kernel & Workflow Engine | `Workflow-Engine` (L2 support), `ScheduledPublish-Processor` (L4), `ReferenceData` (zip/geo, L1), `Token/Platform` (infra) | God processor: unrelated platform responsibilities. |
| S7 | **SRV0010** Reporting & Export | `Reporting-ReadModel` (Domain/projection), `CSV-Export-Processor` (L4) | Read-model vs scheduled export are different layers. |
| S8 | **SRV0007** Payment Processing | keep `Payment Processing` (Domain) + extract `RecurringPayment-Processor` (cron) + delegate gateway calls to S1 adapters | Domain currently initiates vendor calls + side-effects in postSave. |

## 2. MERGE candidates

| # | Merge | Into | Justification |
|---|---|---|---|
| M1 | Slack + Telegram loggers + ES-audit | **SRV0018** (keep as one `Ops-Logging-Adapters`) | Thin near-identical PSR-3 loggers; not worth 3 SRVs. Already merged in discovery — confirm. |
| M2 | `blacklist` data ops | **SRV0003** (already merged) | Passive data written by scoring; no standalone behavior (discovery rule 8). Confirm; expose write as explicit port. |

## 3. RENAME candidates (align name to boundary + responsibility)

| Source | Target name | Reason |
|---|---|---|
| SRV0008 | `PaymentGateway-Port` + `{ComGate,Netopia,MAIB}-Adapter` | boundary-per-adapter naming |
| SRV0009 | `BankReconciliation` (Domain) + explicit inbound adapters | separate domain from transport |
| SRV0002 | `Application-Status-Orchestrator` | already boundary-aligned — keep |
| SRV0016 | `SearchIndex-Processor` + `Elasticsearch-Adapter` | processor vs transport |

## 4. Do-not-touch (already clean for rewrite)

- **SRV0019 Document Import (OneDrive)** — single boundary (MS Graph), correct Adapter shape.
- **SRV0002 Application Status Orchestrator** — correct Orchestrator boundary (only smell is *synchronous* fan-out → runtime concern, not a boundary change).
- **SRV0012 Party & Contact Management**, **SRV0001**, **SRV0005**, **SRV0011** — correct Domain boundaries. (Internal God-class refactors are code-level, out of SRV scope — noted, not a split.)
- **SRV0015 Identity & Access** — coherent identity/security boundary.

## 5. Transitional vs Stable

- **Transitional (legacy-only, do not build into rewrite core):** SRV0006 Campaign Recommendation (dead ML), the ES-audit half of SRV0018, patron_devel debug endpoints referenced in SRV0015.
- **Stable (rewrite core):** all Domain SRVs, SRV0002, the split payment/bank adapters, SRV0013 messaging orchestrator, SRV0019.

> No behavior invented; where a source draft lacked metadata it is marked in [SRV-target-list.md](SRV-target-list.md). `patron.action` provenance and exact per-cron gating remain `Unclear from SRV drafts` (Tier-1/2 gaps in SRV-candidates.md §7) — resolve in FlowInspector/FlowMiner, not here.
