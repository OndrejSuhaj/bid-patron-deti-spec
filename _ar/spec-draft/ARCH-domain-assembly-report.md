# ARCH Domain Assembly Report (Patronus)

> Generated by **AR:ARCHDomainAssembler** · 2026-07-02.

## 1. Active rules and template used

- Rules: [tooling/docs/rules-ARCH.md](../../tooling/docs/rules-ARCH.md),
  [tooling/docs/cross-layer-discipline.md](../../tooling/docs/cross-layer-discipline.md)
  (rules win over template on conflict).
- Template: [tooling/templates/template-ARCH.md](../../tooling/templates/template-ARCH.md)
  (Purpose / System Overview / Structural Components / Interaction Model), extended with a
  navigation-only **Cross-links** section carrying the relatedEN/UC/FN/ES/MSG/BR `doc_id` sets.
- Agent spec: [orchestration/03-spec-driven-documentation/agents/ARCHDomainAssembler.md](../../orchestration/03-spec-driven-documentation/agents/ARCHDomainAssembler.md).

## 2. Domains assembled

10 domain ARCH docs (ARCH0003..ARCH0012), one per bounded context C1..C11 with C10+C11 merged. See
[ARCH-domain-map.md](ARCH-domain-map.md) for the full table. This was a **transformation/consolidation**
pass: no new domains, responsibilities, or boundaries were invented, and no behaviour was
rediscovered from code — the domain set is exactly the ARCH0001 §4 Bounded Context Map.

## 3. Source overview artifacts consumed

- ARCH0001 §4 Bounded Context Map (spine: contexts, owned SRVs, resident aggregates) and §3/§5/§7/§8.
- ARCH0002 Context Interaction Map (§(a) sync, §(b) async, §(c) event/side-effect chains).
- DOMAIN-aggregates.md (AG1..AG13 residency + non-aggregate reference/read-model/content classification).
- Layer maps for cross-links: EN-candidates, UC-candidates (+ UC-srv-traceability), FN-capability-map,
  ES-system-map, MSG-message-map, BR-rule-map; glossary.md for terminology.
- **Not read:** Patronus source under `intake/current-solution/_source/` (per task) and ARCH0001/0002
  were read-only (not overwritten).

## 4. Domains left broader than expected, and why

- **ARCH0012 Platform, Search & Operations (C10 + C11 merged).** The task explicitly permits merging
  genuinely thin/adjacent contexts (Search + Ops-infra). C10 and C11 are the two thinnest contexts
  (HS16): neither owns a resident aggregate, C10 indexes other aggregates as a save-time side-effect,
  and C11 holds only reference/config entities. Their core flows are un-mined. Kept as one navigation
  surface with the four sub-capabilities (search / workflow+publish / reference data / ops alerting)
  individually identified.
- **ARCH0009 Party / CRM kept broad on the overloaded Contact.** The universal party store is one
  capability (Contact + Organisation + all three merge sub-flows) rather than split, mirroring FN0014
  and the fact that Contact is shared mutable state read/written cross-context.

## 5. Missing downstream artifacts that weaken navigation

- **BR-MarketingAndAnalyticsRelay is referenced but not authored** (BR-rule-map §C7/C9 note): the CRM
  re-sync / anti-erasure mechanism has no owning BR file. Navigated as an open gap from ARCH0010, not
  invented.
- **Un-mined flows behind Partial UCs:** UC0018 (FL055 search-sync), UC0020 (FL059 ops listener),
  UC0022 (FL057 scheduled publish), and the RO 2% tax-redirect variant of UC0010 — all surfaced as
  Partial in ARCH0012 / ARCH0008 so a reader knows the navigation bottoms out in incomplete evidence.
- **Aggregate-level Hypotheses** carried from DOMAIN-aggregates (CampaignLog writer, RecurringTransaction
  cancellation semantics, ScoringRecord carrier entity) are noted in the relevant domain docs rather
  than resolved.

## 6. Recommended next step

Run **CrossLayerAuditor** and **RefIntegrityValidator** over the new `ARCH/` docs to confirm no
restatement violations crept in and that every `doc_id` reference resolves (building
`_ar/spec-draft/ARCH/_REGISTRY.md` at publication). Then author the missing
**BR-MarketingAndAnalyticsRelay** to close the one dangling BR reference before SpecFinalGenerator
tiers the layer into `spec-final/BA/ARCH/**`.
