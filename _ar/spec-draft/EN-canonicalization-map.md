# EN Canonicalization Map

Tracks, per entity, which source artifacts were used to canonicalize the EN document and what was
removed/preserved. Refreshed in place per entity on each ENCanonicalizer run — do not duplicate rows
for an entity already listed; update its row instead.

| EN File | Source Artifacts | Removed Technical Traces | Preserved Lifecycle Facts | Notes |
|---|---|---|---|---|
| EN0006_Contact.md | Prior EN0006 draft (Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Open Questions); BR-PartyIdentityAndDeduplication; BR-ScoringAndRiskGating; BR-DataProtectionAndErasure; UC0001_SubmitApplication; UC0003_AssessApplicantRisk; UC0016_MaintainPartyRecords; EN0008_User, EN0018_Organisation (relationship cross-check) | `## Origin` (base_table, revisionable/fieldable flags, `contact.install` note), file paths (`ContactEntity.php`, `ContactEntityStorageSchema.php`), class/method names (`AccountService::register`/`ensureUser`, `ContactRemoveDuplicatesController::delete()`, `ScoringService::setBlacklistType`), field-storage tokens presented as code (`field_name`, `blacklist_type`, `entity_reference`, `list_string`, `phone_number`), framework revision keys (uuid/langcode/vid), `## Technical Fields` (ORM mechanics, ReadOnly/derived-field internals), `## Spec Alignment` scaffolding, "soft entity_reference (no DB FK)" schema phrasing | Universal-party-store purpose; role discriminator distinguishing person roles (fundraiser/child/patron/school/lead/etc.); risk/blacklist classification concept (now cited to BR-ScoringAndRiskGating instead of restating wl_zd/wl_z/wl_n/bl mechanics as entity-owned); creation trigger resolved to UC0001; scoring-driven classification update resolved to UC0003; dedup/merge and hard-delete-on-merge transitions resolved to UC0016 with destructive/non-transactional nature cited to BR-PartyIdentityAndDeduplication; attribute set split into system-managed vs user-provided; relationships to EN0001/EN0002/EN0008/EN0018/EN0023 kept as doc_id references; all three prior Open Questions carried forward (rephrased without code) plus one new one on derived birthdate/gender activity status | No dedicated lifecycle-state enum exists in source beyond a publish flag and the risk classification — modeled as Active / Merged-out (terminal) per cross-layer discipline (EN owns lifecycle states, BR owns the classification-write rule content); full blacklist-value transition table remains Partial and is left as an Open Question rather than invented |
| EN0013_Voucher.md | Prior EN0013 draft (Description/Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Open Questions); BR-VoucherPolicy; UC0006_ConfirmPayment; UC0009_RedeemValidateVoucher; UC-candidates.md (INV10, INV24 rows); EN0009_Transaction, EN0004_Campaign (relationship cross-check) | File path/Evidence link (`VoucherEntity.php`), class/method names (`VoucherEntity`, `TransactionEntity::updateVoucherStatus`, `generateMultipleVouchers`, `VoucherApplyResource`), line-number citations (L371-373, L269-271, L806-819, L116-127), field-storage tokens presented as code (`base_table voucher`, `voucher.install`, `hook_schema`, `getOwner()`, `user_id`), `## Origin`, `## Entity Category`, `## Technical Fields`, `## Spec Alignment` scaffolding, FLW tags moved out of body prose (kept only as trigger resolution input, not restated as evidence citations) | Prepaid gift-code purpose bought via a Transaction and applied to a Campaign; two independent lifecycle dimensions (paid state, redemption state); unpaid→paid transition resolved to UC0006 (Confirm Payment) gated by the purchasing Transaction reaching PAID; paid-not-redeemed→redeemed transition resolved to UC0009 (Redeem/Validate Voucher); owner-derived-from-Transaction rule and redemption/uniqueness/concurrency rules cited to BR-VoucherPolicy instead of restated; attribute set split into system-managed (status, is_applied, applied, expiration, reminded, transaction ref, campaign ref) vs user-provided (name, price, recipient_*, delivery_type); relationships to EN0009/EN0004 kept as doc_id references; all three prior Open Questions carried forward (rephrased without code) | Expiration/reminder job existence remains Missing evidence, not promoted to a canonical transition — kept as a Hypothesis note under State Transitions and repeated as an Open Question; the dual-write ambiguity on the `applied` timestamp (paid-promotion vs redemption) is carried as a Conflict Open Question rather than resolved |
| EN0017_ScoringRecord.md | Prior EN0017 draft (Description/Entity Category/Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Spec Alignment/Open Questions); BR-ScoringAndRiskGating; UC0003_AssessApplicantRisk (UC0003.1/UC0003.2 sub-flows); EN0001_Application, EN0002_ApplicationProfile, EN0006_Contact, EN0016_Blacklist (relationship cross-check) | File path/class/method names (`ScoringForm.php`, `ApplicationStatusUpdateSubscriber.php`, `ScoringLowRiskForm.php`, `ScoringEntity.php`, `ScoringResource.php`, `ScoringService::setBlacklistType`), route/REST resource strings (`/api/scoring/{entity_name}/{entity_id}`, `scoring_rest_resource`), raw DB/schema tokens presented as code (`base_table scoring_entity`, `string_long`, `entity_reference`, `target_id`, `entity_keys`), line-number citations (`submitForm:180`, `ScoringResource.php:88,105`), `## Entity Category`, `## Origin`, `## Technical Fields`, `## Spec Alignment`, `## Allowed Statuses`, `Evidence:` implementation-scaffolding sections | Dual-representation purpose (manual assessment snapshot vs. system-derived low-risk score) held as Application-attached scoring state rather than an independently addressable record; manual-assessment transition resolved to UC0003 (UC0003.1); low-risk recompute transition resolved to UC0003 (UC0003.2); approval-verdict and coordinator-threshold status gates cited to BR-ScoringAndRiskGating instead of restated; blacklist-propagation hazard cited to BR-ScoringAndRiskGating; attribute set split into system-managed (low-risk score/breakdown, assessing user, timestamps) vs user-provided (risk fields, verdict, blacklist classification); relationships to EN0001/EN0002/EN0006/EN0016 kept as doc_id references; all four prior Open Questions carried forward (rephrased without code), one merged into the BR reference | The dormant `scoring_entity`-style carrier (no current-state writer) is preserved as a Status: Planned/dormant note under Purpose rather than a canonical lifecycle state; the manual-vs-low-risk supersedence question remains an unresolved Conflict Open Question |
| EN0020_Partner.md | Prior EN0020 draft (Description/Entity Category/Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Spec Alignment/Open Questions); DOMAIN-kernel.md (Partner row — no UC mapped, "no domain behaviour beyond ordering"); grep-checked BR/, UC-candidates.md, UC/, glossary.md for any Partner coverage (none found); EN0008_User (relationship cross-check) | File paths/class name (`partner/src/Entity/PartnerEntity.php`), raw schema tokens presented as code (`base_table partner`, `list_string`, `er`), `## Entity Category`, `## Origin`, `## Technical Fields`, `## Spec Alignment`, `Evidence:` citations, `## Allowed Statuses` scaffolding | Marketing/display-content purpose (footer "support us" logos); boolean publish flag modeled as an Unpublished/Published lifecycle; `category` kept as a non-lifecycle classification attribute; attribute set split into system-managed (owner, created, changed) vs user-provided (category, name, link, logo, order, published); relationship to EN0008 kept as a doc_id reference; all three prior Open Questions carried forward (rephrased without code) plus a new one flagging the absent UC/BR coverage | No BR or UC document references Partner (confirmed by search) — both state-transition triggers are recorded as Unknown/Not evidenced rather than invented; taxonomy `partners` bundle overlap risk kept as an Open Question, not resolved |
| EN0024_Blog.md | Prior EN0024 draft (Description/Entity Category/Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Spec Alignment/Open Questions); glossary.md (Blog post row → UC0022, no BR); grep-checked BR/, UC-candidates.md, UC/ for dedicated Blog coverage (none found; UC0022 resolves to "RunPlatformWorkflowEngine", not a Blog-specific flow, so not cited as a transition trigger); evidence/flow-index.md (FL057 scheduler row, Partial, not promoted); EN0004_Campaign, EN0008_User (relationship cross-check) | File path/class name (`blog/src/Entity/BlogEntity.php`, ~600 LOC note), other file paths (BlogService/ListBuilder/AccessControlHandler/RouteProvider/ThemeNegotiator, Form/Controller/REST-resource/Views-field classes), method/line citations (`preSave:76`, raw `update blog set is_hero_post=false` SQL, `getOwner:176`, `generateSlug` service call), `## Entity Category`, `## Origin`, `## Technical Fields`, `## Spec Alignment`, `Evidence:` scaffolding, schema-level phrasing ("no unique key declared", "via publishedBaseFieldDefinitions()", "soft ref") | Editorial content-with-CTA purpose incl. distinction from the unrelated core `node.blog` bundle; boolean publish flag modeled as Unpublished/Published lifecycle; independent single-hero selection mechanism kept as a non-lifecycle invariant rather than a per-post state; attribute set split into system-managed (slug, created, changed, status) vs user-provided (name, perex, body, image, gallery, category, is_hero_post, cta_* fields, user_id); relationships to EN0004/EN0008 kept as doc_id references; all four prior Open Questions carried forward (rephrased without code) plus a new one on the absence of any modelled UC for Blog authoring/publishing | No BR or dedicated UC document covers Blog (confirmed by search) — both state-transition triggers are recorded as Hypothesis/Not evidenced rather than invented; the glossary's UC0022 link was checked and rejected as a transition-trigger source since UC0022 is the generic workflow-engine UC, not Blog-specific |
| EN0021_Feedback.md | Prior EN0021 draft (Description/Entity Category/Origin/Core Fields/Technical Fields/Relations/Allowed Statuses/Lifecycle/Spec Alignment/Open Questions); UC0002_OrchestrateApplicationStatusChange (fan-out step 9, feedback-waiting status); UC0011_ManageCampaignStoryLifecycle (checked, no direct Feedback-creation step found); UC-candidates.md (EN0021 row); glossary.md (C093/C108/C109 feedback-status terms); EN0001_Application, EN0004_Campaign, EN0008_User (relationship cross-check); no dedicated BR file exists for this entity | File paths/class names (`feedback/src/Entity/FeedbackEntity.php`, `FundraiserFeedbackForm.php`, `CampaignFeedbackForm.php`), method/line-number citations (`FeedbackEntity::create(...)`, `:109`, `:182`, `:276`), raw DB/schema tokens presented as code (`base_table feedback`, `string_long`, `er ->`, `file, private`), `## Entity Category`, `## Origin`, `## Technical Fields`, `## Spec Alignment`, `Evidence:` implementation-scaffolding sections, "er ->" relationship shorthand | Donor-thank-you purpose authored by a fundraiser for a Campaign; create -> published default preserved; created -> sent (dispatch timestamp) transition preserved; attribute set split into system-managed (`sent`, `status`, author reference, `created`/`changed`) vs user-provided (`name`, `body`, fundraiser, campaign, images); relationships to EN0001/EN0004/EN0008 kept as doc_id references; all three prior Open Questions carried forward (rephrased without code), plus one new question flagging that no confirmed UC step directly models Feedback creation itself | No BR document governs this entity; the adjacent Application feedback-waiting status is cited (UC0002, glossary C093) as the closest confirmed trigger for the related session, but the link from that session to an actual Feedback record remains an open Conflict/Uncertain point, not resolved by invention |

